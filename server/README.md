# Hackers & painter - Server

DB migration commands:

- npm run migrate:create {file} - creates a migration file
- npm run migrate:up - run migration
- npm run migreate:down - revert migration

DB seed [src/modules/*.seed.ts]
